/**
 * Format utilities (legacy - use date.ts and currency.ts for new code)
 * @deprecated Use date.ts and currency.ts instead
 */

// Re-export from date.ts for backward compatibility
export { formatDate, formatDateTime } from './date';

// Re-export from currency.ts for backward compatibility  
export { formatCurrency } from './currency';

