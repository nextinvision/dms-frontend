"use client";
import { ReactNode } from "react";

interface CentralInventoryLayoutProps {
  children: ReactNode;
}

export default function CentralInventoryLayout({ children }: CentralInventoryLayoutProps) {
  return <>{children}</>;
}

